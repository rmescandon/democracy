"use server";

import { ethers } from "hardhat";

export async function createProposal(formData: FormData) {
  // TODO IMPLEMENT: Create a proposal
  console.log("Creating proposal with data:", formData);
}
